package com.test.demo;

import com.alibaba.fastjson.JSON;
import com.test.demo.model.*;
import com.test.demo.repository.*;
import com.test.demo.repository.result.NodeReviewer;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.*;

@RunWith(SpringRunner.class)
@SpringBootTest
public class ApiDemoApplicationTests {
//    @Autowired
//     MessageRepository messageRepository;
//    @Autowired
//     MessageStateRepository messageStateRepository;
//    @Autowired
//     UserRepository userRepository;
//    @Autowired
//    NodeRepository nodeRepository;
    @Test
    public void contextLoads() {

//            System.out.println("signoff:"+ object.getSignoff());

//            System.out.println("object:"+object.getSignoff().getReviewer());
//            System.out.println("object:"+object.getReviewer());
//       MessageState state= messageStateRepository.save(new MessageState(2L,0,new Message(1L)));
//       System.out.println("state:"+state);
//        messageStateRepository.updateByUser(2L,new Message(1L));
//        messageStateRepository.deleteMessageStateByMessageAndUserId(new Message(1L),2L);
    }

}
